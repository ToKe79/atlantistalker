language: c
matrix:
        include:
                - os: linux
                  sudo: required
                  dist: xenial
                  install: ./.travis/install_deps.sh
                  script: ./.travis/compile.sh
